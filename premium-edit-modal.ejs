<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../../sidebar.css">
    <%- include('partials/adminpanelhead', {bot, user, path}) %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://kit.fontawesome.com/b9930c15d0.js" crossorigin="anonymous"></script>
    <title>Premium Düzenleme</title>
</head>

<style>
:root {
    --premium-primary-color: #3b82f6;
    --premium-primary-hover: #2563eb;
    --premium-success-color: #10b981;
    --premium-warning-color: #f59e0b;
    --premium-danger-color: #ef4444;
    --premium-text-color: #1f2937;
    --premium-text-light: #6b7280;
    --premium-border-color: #e5e7eb;
    --premium-bg-color: #f3f4f6;
    --premium-card-bg: #ffffff;
    --premium-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
    --premium-radius: 0.5rem;
    --premium-modal-overlay: rgba(0, 0, 0, 0.5);
}

.home{
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
}

.edit-modal {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: var(--premium-modal-overlay);
    z-index: 2000;
    animation: fadeIn 0.3s ease;
}

@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

.edit-modal-content {
    position: relative;
    background-color: var(--premium-card-bg);
    margin: 3rem auto;
    padding: 2rem;
    border-radius: var(--premium-radius);
    max-width: 600px;
    width: 90%;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
    animation: slideDown 0.3s ease;
}

@keyframes slideDown {
    from {
        transform: translateY(-50px);
        opacity: 0;
    }
    to {
        transform: translateY(0);
        opacity: 1;
    }
}

.edit-modal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 2rem;
    padding-bottom: 1rem;
    border-bottom: 2px solid var(--premium-border-color);
}

.edit-modal-header h2 {
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--premium-text-color);
    margin: 0;
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.close-modal-btn {
    background: none;
    border: none;
    color: var(--premium-text-light);
    cursor: pointer;
    padding: 0.5rem;
    border-radius: 0.375rem;
    transition: all 0.2s;
}

.close-modal-btn:hover {
    background-color: var(--premium-bg-color);
    color: var(--premium-danger-color);
}

.close-modal-btn svg {
    width: 1.5rem;
    height: 1.5rem;
}

.edit-form-group {
    margin-bottom: 1.5rem;
}

.edit-form-group label {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.75rem;
    font-weight: 600;
    color: var(--premium-text-color);
    font-size: 0.95rem;
}

.edit-form-group label svg {
    width: 1.125rem;
    height: 1.125rem;
    color: var(--premium-primary-color);
}

.edit-form-group select,
.edit-form-group input {
    width: 100%;
    padding: 0.875rem;
    border: 2px solid var(--premium-border-color);
    border-radius: var(--premium-radius);
    font-size: 1rem;
    transition: all 0.2s;
    background-color: white;
}

.edit-form-group select:focus,
.edit-form-group input:focus {
    outline: none;
    border-color: var(--premium-primary-color);
    box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.1);
}

.edit-form-group input:disabled {
    background-color: var(--premium-bg-color);
    cursor: not-allowed;
}

.plan-badge-preview {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.5rem 1rem;
    border-radius: 9999px;
    font-weight: 600;
    margin-top: 0.5rem;
    font-size: 0.875rem;
}

.plan-badge-preview.basic {
    background-color: #dbeafe;
    color: #1e40af;
}

.plan-badge-preview.premium {
    background-color: #fef3c7;
    color: #92400e;
}

.plan-badge-preview.professional {
    background-color: #f3e8ff;
    color: #6b21a8;
}

.edit-actions {
    display: flex;
    gap: 1rem;
    margin-top: 2rem;
    padding-top: 1.5rem;
    border-top: 2px solid var(--premium-border-color);
}

.edit-save-btn,
.edit-cancel-btn {
    flex: 1;
    padding: 0.875rem;
    border: none;
    border-radius: var(--premium-radius);
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s;
    font-size: 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
}

.edit-save-btn {
    background-color: var(--premium-success-color);
    color: white;
}

.edit-save-btn:hover {
    background-color: #059669;
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(16, 185, 129, 0.4);
}

.edit-cancel-btn {
    background-color: var(--premium-bg-color);
    color: var(--premium-text-color);
}

.edit-cancel-btn:hover {
    background-color: #d1d5db;
}

.edit-save-btn svg,
.edit-cancel-btn svg {
    width: 1.25rem;
    height: 1.25rem;
}

.user-info-card {
    background-color: var(--premium-bg-color);
    padding: 1rem;
    border-radius: var(--premium-radius);
    margin-bottom: 1.5rem;
    display: flex;
    align-items: center;
    gap: 1rem;
}

.user-info-card img {
    width: 48px;
    height: 48px;
    border-radius: 50%;
}

.user-info-text h3 {
    margin: 0;
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--premium-text-color);
}

.user-info-text p {
    margin: 0.25rem 0 0 0;
    font-size: 0.875rem;
    color: var(--premium-text-light);
    font-family: monospace;
}

.date-info {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    background-color: var(--premium-bg-color);
    padding: 1rem;
    border-radius: var(--premium-radius);
    margin-top: 0.5rem;
}

.date-info-item {
    display: flex;
    flex-direction: column;
}

.date-info-item label {
    font-size: 0.75rem;
    color: var(--premium-text-light);
    margin-bottom: 0.25rem;
    font-weight: 500;
}

.date-info-item span {
    font-size: 0.875rem;
    color: var(--premium-text-color);
    font-weight: 600;
}
</style>

<body>
    <nav class="sidebar close">
        <header>
            <div class="image-text">
                <span class="image">
                    <img src="https://cdn.discordapp.com/avatars/<%= member.id %>/<%= member.avatar %>" alt="">
                </span>

                <div class="text logo-text">
                    <span class="name"><%= member.username %></span>
                    <span class="profession"><%= auth %></span>
                </div>
            </div>

            <i class='bx bx-chevron-right toggle'></i>
        </header>

        <div class="menu-bar">
            <div class="menu">
                <li class="search-box">
                    <i class='bx bx-search icon'></i>
                    <input type="text" placeholder="Ara...">
                </li>

                <ul class="menu-links">
                    <li class="nav-link">
                        <a href="./embed">
                            <i class="fa-solid fa-wand-magic-sparkles icon"></i>
                            <span class="text nav-text">Mesaj Oluştur</span>
                        </a>
                    </li>

                    <li class="nav-link">
                        <a href="./talepler">
                            <i class="fa-solid fa-ticket icon"></i>
                            <span class="text nav-text">Talepler</span>
                        </a>
                    </li>

                    <li class="nav-link">
                        <a href="./forum">
                            <i class="fa-regular fa-comment icon"></i>
                            <span class="text nav-text">Forum</span>
                        </a>
                    </li>

                    <li class="nav-link">
                        <a href="./premium">
                            <i class="fa-solid fa-crown icon"></i>
                            <span class="text nav-text">Premium</span>
                        </a>
                    </li>

                    <li class="nav-link">
                        <a href="./karaliste">
                            <i class="fa-solid fa-user-slash icon"></i>
                            <span class="text nav-text">Karaliste</span>
                        </a>
                    </li>

                    <li class="nav-link">
                        <a href="./ayarlar">
                            <i class="fa-solid fa-sliders icon"></i>
                            <span class="text nav-text">Ayarlar</span>
                        </a>
                    </li>

                    <li class="nav-link">
                        <a href="./kullanicilar">
                            <i class="fa-solid fa-user icon"></i>
                            <span class="text nav-text">Kullanıcılar</span>
                        </a>
                    </li>

                    <li class="nav-link">
                        <a href="./dilekle">
                            <i class="fa-solid fa-language icon"></i>
                            <span class="text nav-text">Dil Ekle</span>
                        </a>
                    </li>

                    <li class="nav-link">
                        <a href="./updates">
                            <i class="fa-solid fa-wrench icon"></i>
                            <span class="text nav-text">Güncelleme</span>
                        </a>
                    </li>
                </ul>
            </div>

            <div class="bottom-content">
                <li class="">
                    <a href="/">
                        <i class='bx bx-home-alt icon'></i>
                        <span class="text nav-text">Ana sayfa</span>
                    </a>
                </li>

                <li class="">
                    <a href="/logout">
                        <i class='bx bx-log-out icon' ></i>
                        <span class="text nav-text">Çıkış yap</span>
                    </a>
                </li>

                <li class="mode">
                    <div class="sun-moon">
                        <i class='bx bx-moon icon moon'></i>
                        <i class='bx bx-sun icon sun'></i>
                    </div>
                    <span class="mode-text text">Koyu mod</span>

                    <div class="toggle-switch">
                        <span class="switch"></span>
                    </div>
                </li>
            </div>
        </div>
    </nav>

    <section class="home">
        <div id="editModal" class="edit-modal">
            <div class="edit-modal-content">
                <div class="edit-modal-header">
                    <h2>
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"></path>
                            <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"></path>
                        </svg>
                        Premium Üyelik Düzenle
                    </h2>
                    <button class="close-modal-btn" onclick="closeEditModal()">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <line x1="18" y1="6" x2="6" y2="18"></line>
                            <line x1="6" y1="6" x2="18" y2="18"></line>
                        </svg>
                    </button>
                </div>

                <div class="user-info-card" id="editUserInfo">
                </div>

                <form id="editPremiumForm">
                    <div class="edit-form-group">
                        <label>
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <circle cx="12" cy="12" r="10"></circle>
                                <circle cx="12" cy="12" r="4"></circle>
                            </svg>
                            Kullanıcı ID
                        </label>
                        <input type="text" id="editUserId" disabled>
                    </div>

                    <div class="edit-form-group">
                        <label>
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
                                <circle cx="12" cy="7" r="4"></circle>
                            </svg>
                            Premium Planı
                        </label>
                        <select id="editPlan" onchange="updatePlanPreview()">
                            <option value="basic">🥉 Basic</option>
                            <option value="premium">🥈 Premium</option>
                            <option value="professional">🥇 Professional</option>
                        </select>
                        <div id="planPreview"></div>
                    </div>

                    <div class="edit-form-group">
                        <label>
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                                <line x1="16" y1="2" x2="16" y2="6"></line>
                                <line x1="8" y1="2" x2="8" y2="6"></line>
                                <line x1="3" y1="10" x2="21" y2="10"></line>
                            </svg>
                            Bitiş Tarihi
                        </label>
                        <input type="date" id="editEndDate">

                        <div class="date-info">
                            <div class="date-info-item">
                                <label>Başlangıç Tarihi</label>
                                <span id="editStartDateDisplay"></span>
                            </div>
                            <div class="date-info-item">
                                <label>Kalan Gün</label>
                                <span id="editRemainingDays"></span>
                            </div>
                        </div>
                    </div>

                    <div class="edit-form-group">
                        <label>
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <path d="M22 12h-4l-3 9L9 3l-3 9H2"></path>
                            </svg>
                            Durum
                        </label>
                        <select id="editStatus">
                            <option value="active">🟢 Aktif</option>
                            <option value="expired">🔴 Pasif</option>
                        </select>
                    </div>

                    <div class="edit-actions">
                        <button type="button" class="edit-cancel-btn" onclick="closeEditModal()">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <line x1="18" y1="6" x2="6" y2="18"></line>
                                <line x1="6" y1="6" x2="18" y2="18"></line>
                            </svg>
                            İptal
                        </button>
                        <button type="submit" class="edit-save-btn">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                <polyline points="20 6 9 17 4 12"></polyline>
                            </svg>
                            Kaydet
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </section>

    <script>
        const body = document.querySelector('body');
        const sidebar = body.querySelector('nav');
        const toggle = body.querySelector(".toggle");
        const searchBtn = body.querySelector(".search-box");
        const modeSwitch = body.querySelector(".toggle-switch");
        const modeText = body.querySelector(".mode-text");

        toggle.addEventListener("click", () => {
            sidebar.classList.toggle("close");
        });

        searchBtn.addEventListener("click", () => {
            sidebar.classList.remove("close");
        });

        modeSwitch.addEventListener("click", () => {
            body.classList.toggle("dark");
            if (body.classList.contains("dark")) {
                modeText.innerText = "Aydınlık";
            } else {
                modeText.innerText = "Karanlık";
            }
        });

        let currentEditingUser = null;

        function openEditModal(userId, username, avatar, plan, startDate, endDate, status) {
            currentEditingUser = {
                id: userId,
                username: username,
                avatar: avatar,
                originalPlan: plan,
                originalStartDate: startDate,
                originalEndDate: endDate,
                originalStatus: status
            };

            document.getElementById('editUserInfo').innerHTML = `
                <img src="${avatar}" alt="${username}">
                <div class="user-info-text">
                    <h3>${username}</h3>
                    <p>${userId}</p>
                </div>
            `;

            document.getElementById('editUserId').value = userId;
            document.getElementById('editPlan').value = plan;
            document.getElementById('editStatus').value = status;

            const start = new Date(startDate);
            const end = new Date(endDate);

            document.getElementById('editStartDateDisplay').textContent = start.toLocaleDateString('tr-TR');
            document.getElementById('editEndDate').value = end.toISOString().split('T')[0];

            updateRemainingDays();
            updatePlanPreview();

            document.getElementById('editModal').style.display = 'block';
        }

        function closeEditModal() {
            document.getElementById('editModal').style.display = 'none';
            currentEditingUser = null;
        }

        function updatePlanPreview() {
            const plan = document.getElementById('editPlan').value;
            const preview = document.getElementById('planPreview');

            const planNames = {
                basic: '🥉 Basic',
                premium: '🥈 Premium',
                professional: '🥇 Professional'
            };

            preview.innerHTML = `<div class="plan-badge-preview ${plan}">${planNames[plan]}</div>`;
        }

        function updateRemainingDays() {
            const endDate = new Date(document.getElementById('editEndDate').value);
            const today = new Date();
            const remainingDays = Math.ceil((endDate - today) / (1000 * 60 * 60 * 24));

            document.getElementById('editRemainingDays').textContent = `${remainingDays} gün`;
        }

        document.getElementById('editEndDate').addEventListener('change', updateRemainingDays);

        document.getElementById('editPremiumForm').addEventListener('submit', async (e) => {
            e.preventDefault();

            const updatedData = {
                userId: document.getElementById('editUserId').value,
                plan: document.getElementById('editPlan').value,
                endDate: document.getElementById('editEndDate').value,
                status: document.getElementById('editStatus').value
            };

            console.log('Güncellenen veri:', updatedData);

            alert('Premium üyelik başarıyla güncellendi!');
            closeEditModal();
        });

        window.onclick = function(event) {
            const modal = document.getElementById('editModal');
            if (event.target === modal) {
                closeEditModal();
            }
        }
    </script>
</body>
</html>
